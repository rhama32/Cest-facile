<% if current_doctor.approved? %>
  <div class="container">
    <div class="row">
    <div class="offset-0 col-xs-8 offset-xs-2">
      <h4 class="text-center"><b>予約履歴詳細画面</b></h4>
      <h5 class="text-center"><b><%= @admin_customer.last_name + @admin_customer.first_name %>さんの予約詳細</b></h5>
        <table class="mt-4 col-md-6 table d-grid col-md-8 mx-auto text-center">
          <thead>
            <tr class="d-grid col-md-8 mx-auto">
              <th>予約日</th>
              <th><%= @reservation.hope_day.strftime("%Y年 %m月 %d日") %></th>
            </tr>
            <tr class="d-grid col-md-8 mx-auto">
              <th>予約時間</th>
              <th><%= @reservation.hope_time.strftime("%H:%M") %></th>
            </tr>
            <tr class="d-grid col-md-8 mx-auto">
              <th>予約院</th>
              <th><%= @reservation.hospital.name %></th>
            </tr>
            <tr class="d-grid col-md-8 mx-auto">
              <th>予約ステータス</th>
              <th>
                <% if @reservation.status == 'reservation_pending' %>
                  <p class="text-success font-weight-bold">申請中</p>
                <% elsif @reservation.status == 'reservation_confirmed' %>
                  <p class="text-success font-weight-bold">予約確定</p>
                <% elsif @reservation.status == 'tentative_reservation' %>
                  <p class="text-secondary font-weight-bold">仮予約中</p>
                <% else %>
                  <p class="text-secondary font-weight-bold">受付キャンセル</p>
                <% end %>
              </th>
            </tr>
          </thead>
        </table>
        <div class="mt-3 text-center">
          <%= link_to '会員予約一覧に戻る', doctor_reservations_path, class: "btn btn-success" %>
        </div>
      </div>
    </div>
  </div>
<% else %>
  申請の承認をお待ちください。
<% end %>