<div class="container">
  <div class="row-lg-11">
    <div class="col-3">
      <h4 class="bg-light text-center">医療関係者ログイン</h4>
    </div>
  </div>
        <div class="row-lg-11">
          <div class="col-5">
            <h5 class="bg-light text-center">医療関係者の方は<br>
            こちらからログイン</h5>
          </div>
        </div>
              <%= form_for(resource, as: resource_name, url: doctor_session_path) do |f| %>
                <table class="table table-borderless">
                  <tbody>
                    <tr>
                      <td>メールアドレス</td>
                      <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
                    </tr>
                    <tr>
                      <td>パスワード</td>
                      <td><%= f.password_field :password, autocomplete: "current-password" %></td>
                    </tr>
                    <tr>
                      <% if devise_mapping.rememberable? %>
                        <td>ログイン情報を記憶する</td>
                        <td><%= f.check_box :remember_me %></td>
                      <% end %>
                    </tr>
                    <tr>
                      <td><%= f.submit "ログイン", class:"btn btn-success center-block" %></td>
                    </tr>
                  </tbody>
                </table>
              <% end %>
            <h4 class="bg-light">新規医療者登録がお済みでない方</h4>
          <%= render "doctor/shared/links" %>から新規登録を行ってください。
        </div>