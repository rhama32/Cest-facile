<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h4 class="mt-3"><b>医療関係者ログイン</b></h4>
      <h5 class="mt-3"><b>医療関係者の方は<br>こちらからログイン</b></h5>
    </div>
      <%= form_for(resource, as: resource_name, url: doctor_session_path) do |f| %>
        <table class="table table-borderless">
          <tbody>
            <tr>
              <td>メールアドレス</td>
              <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
            </tr>
            <tr>
              <td>パスワード</td>
              <td><%= f.password_field :password, autocomplete: "current-password" %></td>
            </tr>
            <tr>
              <% if devise_mapping.rememberable? %>
                <td>ログイン情報を記憶する</td>
                <td><%= f.check_box :remember_me %></td>
              <% end %>
            </tr>
            <tr>
              <td><%= f.submit "ログイン", class:"btn btn-success center-block" %></td>
            </tr>
          </tbody>
        </table>
      <% end %>
    <h5 class="mt-3"><b>新規医療者登録がお済みでない方</b></h5>
    <div class="mt-2"><%= render "doctor/shared/links" %></div>
    <div class="mt-2">から新規登録を行ってください。</div>
  </div>
</div>