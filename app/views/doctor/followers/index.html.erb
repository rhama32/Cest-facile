<% if current_doctor.approved? %>
  <div class="row">
    <div class="col-md-12 text-center">
      <h3 class="mt-3 text-center"><b><%= @doctor.name %>さんのフォロワー一覧</b></h3>
      <h3 class="mt-2 text-center"><b>(全<%= @followers.count %>人)</b></h3>
    </div>
    <div class="col-xs-6">
      <table class="mt-4 table table-hover table-bordered">
        <thead>
        <% @followers.each do |follow| %>
          <tr>
            <td>会員ID</td>
            <td>会員氏名</td>
          </tr>
        </thead>
        <tbody>
          <tr>
            <th><%= follow.customer.id %></th>
            <th><%= follow.customer.full_name %></th>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <div class="d-grid gap-2 col-3 mx-auto text-nowrap">
    <%= link_to '　予約一覧　', doctor_reservations_path, class: "mt-3 btn btn-primary ml-5 text-center" %>
    <%= link_to 'コメント一覧', doctor_reviews_path, class: "mt-3 btn btn-success ml-5 text-center" %>
  </div>
<% else %>
  申請の承認をお待ちください。
<% end %>