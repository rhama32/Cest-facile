<!--部分テンプレート-->
<div class= "container">
  <div class- "row">
    <div class="col-md-4">
      <div class="text-center">ジャンル検索</div>
        
            <% @genres.each do |genre| %>
              <%= link_to genre.name, public_hospitals_path(genre_id: genre.id), class: "text-secondary" %>
   
            <% end %>
      
    </table>
  
</div>