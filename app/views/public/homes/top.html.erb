<!--ジャンル検索-->
<%= render 'public/hospitals/genrevar', genres: @genres %>
<link rel="stylesheet" href="homes.scss" />
<div class="text-center">
<h2>
  Cest-facile</br>
  誰でも手軽に安心を<br/>
  <%= link_to "〜About〜", about_path, class: "font-color2" %>
</h2>
</div>

<div class="text-center">
      <h3 style="margin: 20px 0;">新着病院</h3>
       <div class="row">
        <% @hospitals.each do |hospital| %>
         <div class="col-md-3 mt-5 ">
         <div class="w-95 h-95 shadow">
          <%= link_to public_hospital_path(hospital) do %>
          <%= image_tag hospital.image, size: '180x180' %>
         <div><%= hospital.name %></div>
          <% end %>
         </div>
         </div>
         <% end %>
       </div>
      </div>
  <div class="text-right">
   <%= link_to "全ての病院を見る> ", public_hospitals_path %>
  </div>
  <%#= paginate @hospital %>